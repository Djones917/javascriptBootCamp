// === is the equality operator
//let temp = 31;
//let isFreezing = temp === 32;

//console.log(isFreezing);


// !== is the not equal operator
//let temp = 31;
//let isFreezing = temp !== 32;

//console.log(isFreezing);


// < is the less than operator
//let temp = 31;
//let isFreezing = temp < 32;

//console.log(isFreezing);


// < is the less than or equal to operator
//let temp = 31;
//let isFreezing = temp <= 32;

//console.log(isFreezing);


// < is the greater than operator
let temp = 180;

if (temp <= 32) {
   console.log('It is freezing outside!');
}

if (temp >= 110) {
    console.log('It is hot outside!');
}


// < is the less than or equal to operator
//let temp = 31;
//let isFreezing = temp <= 32;

//console.log(isFreezing);


// Challenge area

// create variable set to your age
let age = 4;
// calculate is child - if they are 7 or under
//let isChild = age <= 7;
// calculate is senior - if they are 65 or older
//let isSenior = age >= 65;
// print is child
//console.log(isChild);
// print is senior
//console.log(isSenior);

if (age <= 7) {
   console.log('Child admision is $4.99');
}

if (age >= 65) {
   console.log('Senior admission is $2.99');
}